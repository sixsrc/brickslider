# Framework Name Quick Start

Use this template when contributing a framework usage guide for BrickSlider.

## Goal

Explain how to use the core BrickSlider package in a real project without changing the library into a framework-specific wrapper.

## Install

```bash
pnpm add @sixsrc/brick-slider
```

```bash
npm install @sixsrc/brick-slider
```

## Example

```tsx
// Add a minimal, working example here.
```

## Notes

- Explain where the slider is mounted.
- Explain how cleanup works.
- Mention any framework-specific lifecycle details.
- Keep the guide focused on core usage unless the tutorial is explicitly about a plugin.
